using UnityEngine;

public class BulletView : MonoBehaviour
{
    // Вызывается, когда объект берётся из пула
    public void OnSpawn()
    {
        // Сейчас ничего не нужно
        // Оставлено как точка расширения на будущее
    }

    // Вызывается перед возвратом в пул
    public void OnDespawn()
    {
        // Сейчас ничего не нужно
        // Оставлено как точка расширения на будущее
    }
}